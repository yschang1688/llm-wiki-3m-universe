# gitleaks config — 沿用內建規則，僅加 raw/ 路徑白名單
# raw/ 為第三方原始素材，pattern 命中皆為雜訊（asset ID、JWT 教學範例 key）
[extend]
useDefault = true

[allowlist]
description = "raw/ 第三方原始素材，排除 secret 掃描"
paths = [
  '''^raw/''',
]
